#ifndef MAIN_H
#define MAIN_H

#include <bits/stdc++.h>

#define MAXN 20
#define INF 100000000
#define NDIR 4
#define MAX_DEPTH 10
#define RANGE 50
#define KSEED 10
#define EZONE 2
#define SHIELD_SCORE 10

#define dist(a, b, c, d) (abs((a) - (c)) + abs((b) - (d)))

const int
    dx[] = {-1, 1, 0, 0},
    dy[] = {0, 0, -1, 1};

using namespace std;

#endif // MAIN_H